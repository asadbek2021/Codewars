

function x(n) {
  const result = [];
  for(let i = 0 ; i < n; i++) {
    result.push(new Array(n).fill(0));
  }
  for(let i = 0 ; i < n; i++) {
    result[i][i]= 1;
    result[i][(n-1)-i]= 1;
  }
   return result;
}
